# AI vs İnsan Metin Dedektörü

Turkish text classifier to detect whether a given text is written by a human or generated by AI.

## Sistem Gereksinimleri

- Python 3.8+
- Windows, macOS, or Linux

## Kurulum (Setup)

### 1. Python Virtual Environment Oluştur

```bash
# Windows
python -m venv venv
.\venv\Scripts\Activate.ps1

# macOS / Linux
python3 -m venv venv
source venv/bin/activate
```

### 2. Bağımlılıkları Yükle

```bash
pip install -r requirements.txt
```

### 3. NLTK Verisini İndir (İlk çalıştırmada otomatik)

Proje ilk çalıştırıldığında NLTK tokenizer'ı otomatik indirilir. Eğer manuel indirmek isterseniz:

```bash
python -c "import nltk; nltk.download('punkt'); nltk.download('punkt_tab')"
```

## Kullanım

### GUI (Grafik Arayüz) - Önerilen

```bash
python gui.py
```

**Özellikler:**
- Metni yapıştırın
- `Tahmin Yap` → 3 model (LR, RF, SVM) için AI/İnsan oranlarını görün
- `Öğret` → Yeni metin ekleyip eğit (arka planda çalışır)
- `Temizle` → Formu temizle

### CLI (Komut Satırı)

```bash
python src/predict.py "Metin buraya" lr
```

Çıktı örneği:
```
{'label': 0, 'proba': 0.123}
```

- `label`: 0=İnsan, 1=AI
- `proba`: AI olasılığı (0.0-1.0)

### Modelleri Yeniden Eğit

```bash
python src/train_ml.py
```

**Gereklilikler:**
- `data/processed/dataset.csv` (başlık: `text,label`)
- Minimum 2 örnek (1 insan, 1 AI)
- Dengeli veri önerilir

## Dosya Yapısı

```
ai_human_detector/
├── gui.py                    # GUI uygulaması
├── requirements.txt          # Python bağımlılıkları
├── README.md                 # Bu dosya
├── data/
│   ├── raw/
│   │   ├── ai_generated.csv  # AI yazıları (8277 satır)
│   │   └── human_Created.csv # İnsan yazıları (3000 satır)
│   ├── clean/
│   │   └── arxiv_3000_clean.csv
│   └── processed/
│       └── dataset.csv       # Eğitim seti (3616 satır)
├── models/
│   ├── lr.pkl               # Logistic Regression
│   ├── rf.pkl               # Random Forest
│   ├── svm.pkl              # Support Vector Machine
│   └── tfidf.pkl            # TF-IDF Vectorizer
└── src/
    ├── train_ml.py          # Eğitim scripti
    ├── predict.py           # Tahmin fonksiyonu
    ├── features.py          # Feature engineering
    ├── clean.py             # Text cleaning
    └── __pycache__/
```

## Modeller & Performans

**Test Seti:** 1194 örnek (997 insan, 197 AI)

| Model | Accuracy | Precision | Recall | F1-Score |
|-------|----------|-----------|--------|----------|
| Logistic Regression | 100% | 1.00 | 1.00 | 1.00 |
| Random Forest | 99% | 1.00 | 0.97 | 0.98 |
| SVM | 100% | 0.99 | 1.00 | 1.00 |

## Sorun Giderme

### Modeller Yüklenmiyor
- Kontrol edin: `models/` klasöründe `lr.pkl`, `rf.pkl`, `svm.pkl`, `tfidf.pkl` var mı?
- Eğitim çalıştırın: `python src/train_ml.py`

### NLTK Tokenizer Hatası
- Otomatik indirilir. Eğer sorun olursa:
```bash
python -c "import nltk; nltk.download('punkt_tab'); nltk.download('punkt')"
```

### CSV Dosyası Yapısı
`dataset.csv` başlık satırı: `text,label`
- `text`: Metin içeriği
- `label`: 0 (insan) veya 1 (AI/makine)

### Eğitim Başarısız
- Minimum 2 satır veri gerekli
- CSV encoding UTF-8 olmalı
- Metinler çok kısa olmamalı (en az 10 kelime)

## Dosya Transferi Notları

**Taşınabilirlik:** Tüm yollar relatif (hardcoded yok). Klasörü herhangi bir yere kopyalayabilirsiniz.

**Veriler:**
- `data/raw/*.csv` — eğitim verileri (başlangıç)
- `data/processed/dataset.csv` — işlenen veri (eğitim sırasında oluşturulur)

**Modeller:**
- `models/*.pkl` — eğitilmiş modeller (binary, sistem bağımsız)

---

**Sorular?** Lütfen bir issue açın veya arkadaşınıza sorun.

Eğlenin! 🚀
